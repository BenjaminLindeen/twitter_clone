if totalPages > 1
    if currentPage > 1
        a(href=`?page=${currentPage - 1}`).prev-btn Previous
    span.page Current Page: #{currentPage}
    if currentPage < totalPages
        a(href=`?page=${currentPage + 1}`).next-btn  Next